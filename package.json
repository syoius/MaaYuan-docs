{
  "scripts": {
    "docs:dev": "vitepress dev --host",
    "docs:build": "vitepress build && node scripts/copy-vercel-config.mjs",
    "docs:preview": "vitepress preview",
    "sync:maayuan-release-note": "node scripts/sync-maayuan-release-note.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  },
  "dependencies": {
    "vue": "^3.5.17"
  }
}
